---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets'; // Użyjemy Image dla optymalizacji
import { getLocale, t } from '@/i18n';
import BlogCard from '@/components/BlogCard';
import LocalizedText from '@/components/LocalizedText.tsx';

// Pobierz wszystkie wpisy, sortuj od najnowszych
const allPosts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

// Wydziel najnowszy post i resztę
const locale = getLocale();
const featuredPost = allPosts[0];
const otherPosts = allPosts.slice(1);

// Funkcja formatująca datę
const formatDate = (date: Date) => {
  const loc = locale === 'pl' ? 'pl-PL' : locale === 'de' ? 'de-DE' : locale === 'it' ? 'it-IT' : locale === 'zh' ? 'zh-CN' : 'en-US';
  return date.toLocaleDateString(loc, {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
}
---
<Layout title={t('nav.blog') + ' - Gracjan'} description={t('nav.blog') + ' — ' + 'Najnowsze wpisy / Latest posts'}>
  <div class="container mx-auto px-4 py-16">
    <header class="mb-16 text-center">
      <LocalizedText client:load as="h1" id="page-heading" tabindex="-1" class="text-4xl md:text-5xl font-bold text-foreground mb-4" k="nav.blog" />
      <LocalizedText client:load as="p" class="text-lg text-muted-foreground" k="blog.subtitle" />
    </header>

    {featuredPost && (
      <section class="mb-16">
        <a href={`/blog/${featuredPost.slug}/`} class="block group focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 focus:rounded-lg">
          <article class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
            {featuredPost.data.heroImage && (
              <div class="overflow-hidden rounded-lg">
                <img 
                  src={featuredPost.data.heroImage} 
                  alt="" 
                  class="w-full h-64 md:h-80 object-cover transition-transform duration-300 group-hover:scale-105"
                  loading="lazy" 
                />
              </div>
            )}
            <div class={featuredPost.data.heroImage ? "" : "md:col-span-2 text-center"}>
              <p class="text-sm text-muted-foreground mb-2">
                {formatDate(featuredPost.data.pubDate)}
              </p>
              <h2 class="text-2xl md:text-3xl font-semibold mb-3 text-foreground group-hover:text-primary transition-colors">
                {featuredPost.data[`title_${locale}`] || featuredPost.data.title}
              </h2>
              <p class="text-md text-foreground/80 mb-4 line-clamp-3">
                {featuredPost.data[`description_${locale}`] || featuredPost.data.description}
              </p>
              <span class="text-sm font-medium text-primary group-hover:underline">
                Czytaj dalej →
              </span>
            </div>
          </article>
        </a>
      </section>
    )}

    {otherPosts.length > 0 && (
      <section>
        <h2 class="text-2xl font-semibold mb-8 text-foreground">{t('blog.allPosts')}</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
          {otherPosts.map((post) => (
            <BlogCard
              slug={post.slug}
              title={post.data[`title_${locale}`] || post.data.title}
              description={post.data[`description_${locale}`] || post.data.description}
              heroImage={post.data.heroImage}
              pubDate={post.data.pubDate.toISOString()}
              tags={post.data.tags}
            />
          ))}
        </div>
      </section>
    )}

    {allPosts.length === 0 && (
      <p class="text-center text-muted-foreground py-16">{locale === 'pl' ? 'Na razie brak wpisów na blogu.' : 'No posts yet.'}</p>
    )}
  </div>
</Layout>

<style>
  /* Usunięto style Y2K dla nagłówka i NeonGradientCard */
  /* Można dodać inne subtelne style, jeśli potrzebne */
</style> 